
import PropTypes from 'prop-types';

const Dashbord = props => {
    return (
        <div className='text-2xl'>
            <h1>Wellcome to dash bord</h1>
        </div>
    );
};

Dashbord.propTypes = {
    
};

export default Dashbord;